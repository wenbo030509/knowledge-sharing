# CoT Compressed 数据交付-学科与推理

> 项目周期：2026-06 ~ 2026-07（3 周）
>
> 状态：🟢 已完成交付，持续深挖中

---

## 一、项目概览

### 背景

构建新的 Pretrain Eval Set。现有 Eval Set 不足以反映模型在**长尾知识 / Reasoning / Agent 链路**上的表现。本批次聚焦 Reasoning 和长尾知识部分的评测数据建设。

### 你的角色

负责 CoT Compressed 数据的质量保证，独立设计并搭建了端到端的质检和修复 Pipeline。

### 交付数据

| 指标 | 数值 |
|------|------|
| 交付总量 | 5,497 条 |
| 合格数据 | 4,346 条（79.06%） |
| 废弃数据 | 1,151 条（20.94%） |
| 黑盒占比 | 872 条 / 20.06% |

### 学科分布

| 学科 | 合格数量 | 占比 | 黑盒数量 | 黑盒占比 |
|------|----------|------|----------|----------|
| 数学（Math） | 2,911 | 66.98% | 582 | 19.99% |
| 物理（Physics） | 692 | 15.92% | 139 | 20.09% |
| 化学（Chemistry） | 562 | 12.93% | 114 | 20.28% |
| 生物（Biology） | 169 | 3.89% | 34 | 20.12% |
| 其他（Other） | 12 | 0.28% | 3 | 25.00% |

---

## 二、完整数据流

```text
[数据来源]
├── 算法工程师学科竞赛题目 + 合成 CoT（人工修改）
└── RL 训练集抽取 → 初版 CoT 压缩
            │
            ↓
[众包团队] 人工修复 CoT
            │
            ↓
[你的 Quality Gate Pipeline] ←── 你搭建的
    │
    ├── Gate 1: 格式验证
    │   输出：合格 / 简单废弃
    │
    ├── Gate 2: 内容验证
    │   输出：合格 / 不合格（进入修复）
    │
    ├── Gate 3: Pipeline 修复（LLM Repair）
    │   输出：修复成功 → 合格 / 修复失败 → 废弃
    │
    ├── Gate 4: 学科标签纠错
    │   ├── Step 1: 硬规则匹配（query 关键词）
    │   ├── Step 2: LLM 推理生成
    │   └── Step 3: 算法标签 vs 你的标签 → 一致=通过 / 不一致=人工审判
    │
    └── Gate 5: 黑盒数据验证
        输出：各学科黑盒占比 ≈ 20%
            │
            ↓
[交付算法团队] → 纳入 Pretrain Eval Set
```

---

## 三、Technical Anatomy（按主题）

### 主题 1：Quality Gate System — 多层质量关卡设计

你设计的五层 Gate 是一个 **Cascading Classification System**：

```text
Layer 1: Deterministic Rules (确定性规则)
    ├── 做什么：格式校验、字段完整性、数据类型检查
    ├── 特点：零成本、零延迟、100%可复现
    └── 处理：直接通过 / 直接废弃

Layer 2: Semantic Rules (语义规则)
    ├── 做什么：CoT 是否保留关键推理步骤？Answer 与 Reasoning 是否一致？
    ├── 特点：需要理解内容，但仍可写成规则
    └── 处理：通过 / 标记为"不合格"进入修复

Layer 3: Probabilistic Repair (概率性修复)
    ├── 做什么：LLM 驱动的 CoT 修复/补全
    ├── 特点：灵活但不确定，需要验证修复结果
    └── 处理：修复后 → 回到 Gate 2 重新验证

Layer 4: Label Correction with Adjudication (标签纠错 + 仲裁)
    ├── 做什么：两步漏斗（硬规则 → LLM）修正学科标签，分歧 case 人工仲裁
    ├── 特点：跨源交叉验证
    └── 处理：一致→通过 / 不一致→人工审判

Layer 5: Distribution Check (分布校验)
    ├── 做什么：验证各学科黑盒占比稳定在 20%
    ├── 特点：统计层面的质量保证
    └── 处理：偏差过大 → 调整配比
```

### 主题 2：标签纠错的两步漏斗

```text
Step 1: 硬规则匹配
├── 方法：query 中提取学科关键词
│   例："求导数" → Math / "牛顿定律" → Physics
├── 优势：快速、确定、零成本
├── 局限：覆盖不完整、跨学科 case 无法处理
└── 覆盖比例：[待补：你实际走了多少条规则匹配？]

Step 2: LLM 推理生成
├── 方法：规则匹配不上的 → LLM 推理学科归属
├── 优势：灵活、覆盖模糊和跨学科 case
├── 局限：有概率出错、成本高于规则
└── 覆盖比例：[待补：有多少条走了 LLM？]

Step 3: 人工仲裁
├── 触发条件：算法标签 ≠ 你的标签
├── 作用：最终权威判定 + 积累规则优化方向
└── 案例数：[待补：有多少条需要人工介入？]
```

**设计原理：** 这是经典的 ML 工程模式 — 确定性 → 概率性 → 人类。每一层处理自己能确定的部分，不确定的留给下一层。

### 主题 3：黑盒数据 20% 的统计学含义

为什么 20%？

```text
如果黑盒太少（如 5%）：
├── 统计推断不稳定，单个学科的样本量太小
└── 防过拟合能力弱

如果黑盒太多（如 50%）：
├── 白盒不够，无法做细致的 reasoning error analysis
└── 丧失了 Eval Set 的诊断价值

20% 是一个经验平衡点：
├── 足够做统计显著性检验
├── 各学科的子样本量可接受（如数学 582 条 → 95% 置信度下误差约 ±4%）
└── 不牺牲白盒的分析能力
```

### 主题 4：CoT 压缩与 Learnability

```text
CoT 压缩的本质问题：

压缩前          压缩后
Question         Question
  ↓                ↓
[详细推理]  →   [关键推理步骤]
  ↓                ↓
Answer           Answer

核心矛盾：
├── 压缩越多 → Token 越少，但 Learning Signal 可能丢失
└── 压缩越少 → Learning Signal 完整，但成本高、效率低

质检的终极标准：
不是"格式对不对"，而是"压缩后模型还能不能从中学到推理能力"

这直接对应 Information Bottleneck 理论：
min I(X; T) - β · I(T; Y)
    ↑               ↑
  压缩程度      保留的预测能力
```

---

## 四、知识树干节点

本项目涉及的知识树节点及其生长状态：

```text
Data / Foundational Data
├── Validation 🌿 ← 你实现了多层 Quality Gate
├── Cleaning 🌿 ← LLM Repair = LLM 增强版数据清洗
├── Transformation 🌿 ← Label Correction 是数据转换
├── Sampling 🌱 ← 黑盒配比背后的采样策略
└── Learnability 🌿 ← CoT 压缩的核心

Data / Evaluation
├── Benchark Design 🌿 ← 你在建设 Eval Set 的基础设施
└── Insight & Analytics 🌱

Data / Strategy
└── Data Mixture 🌿 ← 20% 黑盒配比

System / Workflow Engine
├── Retry & Backoff 🌱
├── State Management 🌱
└── Error Handling 🌱

Learning / Representation Learning
└── Information Bottleneck 🌿 ← CoT 压缩的数学基础
```

---

## 五、待深挖方向

以下是从 PM 视角需要向算法/工程视角补的方向：

### 算法视角待补

- [ ] 质检规则的有效性如何量化？（Precision / Recall）
- [ ] 两步漏斗每层的 accuracy / confusion matrix
- [ ] 修复后的数据分布 vs 修复前的数据分布有没有偏移？
- [ ] 黑盒 20% 的统计功效（statistical power）计算
- [ ] LLM Repair 的 false positive / false negative 分析
- [ ] CoT 压缩质量的自动化评估指标（如何判断"压缩得好不好"？）

### 工程视角待补

- [ ] Pipeline 的并发处理方案
- [ ] 修复失败的 retry 策略和幂等性设计
- [ ] 数据状态的流转追踪（一条数据从进入到离开经历了哪些状态变化）
- [ ] 质检规则的版本管理（规则改了之后历史数据怎么处理）
- [ ] 大规模化：如果数据量翻 10 倍，Pipeline 瓶颈在哪？

---

## 六、时间线

| 日期 | 进展 |
|------|------|
| 2026-06 | 项目启动，搭建 Pipeline |
| 2026-07-10 | 建立学习系统，Day 0 |
| 2026-07-11 | 完整汇报项目，首次 Technical Anatomy：Quality Gate System + 两步漏斗 + 黑盒 20% |
| 待续 | |

---

## 七、四视角展开

### PM 视角

```text
├── 把"什么是好数据"定义成 5 层 Gate（显式化质量标准）
├── 作为数据质量唯一负责人打通闭环（众包 → 质检 → 修复 → 交付）
├── 分级处理而非一刀切（通过/修复/废弃三级分类）
└── 交付物直接可用（纳入预训练 Eval Set）
```

### 算法视角

```text
├── 量化指标：合格率 79.06%（5,497 → 4,346），黑盒占比 20.06%
├── 质检规则有效性待量化（Precision/Recall，见待深挖）
├── 黑盒 20% 的统计学含义：95% 置信度下误差约 ±4%
├── CoT 压缩的 Learnability 标准（Information Bottleneck）
└── 修复后数据分布偏移风险（修复是否引入偏差）
```

### 工程视角

```text
├── Pipeline 工程六维度：幂等性/断点续跑/错误隔离/可观测性/背压/状态
├── 确定性层（规则）→ 概率性层（LLM）→ 人类层（仲裁）的分层成本控制
├── LLM Repair 的失败处理（修复失败 → 废弃，不阻塞管线）
└── 规则版本管理（规则改了历史数据怎么办）
```

### 策略视角

```text
├── 每层 Gate 的必要性验证（Ablation：去掉某层会怎样）
├── 黑盒配比的实验依据（5%/20%/50% 的权衡）
├── 合格率 79% 的解读（是太低还是合理？取决于修复成本曲线）
└── 交付质量的反馈回路（算法团队怎么用这批数据）
```

---

## 八、面试故事

### 版本 A（30 秒）

> 我在字节独立设计了一套 CoT 数据的多层级联分类质量门控系统：5 层 Gate（格式 → 内容 → LLM 修复 → 标签纠错 → 分布校验），处理 5,497 条数据，输出 4,346 条合格（79.06%），交付算法团队纳入预训练 Eval Set。

### 版本 B（2 分钟）

> 背景是预训练 Eval Set 建设需要高质量 CoT 数据，但众包产出存在格式不合规、推理链不完整、学科标签错误等问题，人工质检成本高且标准不一致。
>
> 我独立设计了 5 层 Gate 的级联分类系统：第 1 层格式验证（规则，零成本）、第 2 层内容验证（CoT 是否保留关键推理步骤）、第 3 层 LLM 修复（不合格数据自动化修复后回流重验）、第 4 层学科标签纠错（硬规则 + LLM 两步漏斗，分歧人工仲裁）、第 5 层黑盒分布校验（各学科黑盒占比稳定在 20%，防评测集过拟合 + 保障统计显著性）。
>
> 结果：5,497 条输入 → 4,346 条合格（79.06%），覆盖数学/物理/化学/生物四学科，打通众包 → 质检 → 修复 → 交付闭环。

### 版本 C（5 分钟）— 版本 B + 四视角展开

```text
PM 视角：把"什么是好数据"显式化为 5 层 Gate 标准
算法视角：79.06% 合格率的解读；黑盒 20% 的统计含义
         （95% 置信度 ±4%）；CoT 压缩的 Learnability 标准
工程视角：确定性 → 概率性 → 人类的分层设计（成本/可靠性递进）；
         LLM Repair 失败不阻塞管线
策略视角：每层 Gate 的 ablation 必要性；修复成本曲线；
         交付反馈回路

反思与局限：
├── 质检规则有效性未量化（Precision/Recall）
├── 修复是否引入数据分布偏移未验证
├── 规则版本管理缺失
└── 若重新做：先定义每层 Gate 的量化指标，再加规则版本化
```

---

## 九、面试官追问准备

**Q1：为什么用级联分类而不是一个 LLM 全量判断？**
成本和可靠性分层：规则层零成本处理确定 case（格式问题不需要 LLM），LLM 只处理模糊 case（语义、修复），人类只仲裁分歧。每一层处理自己能确定的，不确定的下沉——这是成本最优 + 可靠性最高的设计。

**Q2：合格率 79% 意味着什么？高还是低？**
取决于修复成本曲线：79% 是"规则直接通过 + LLM 修复成功"的合计。如果修复成本低，可以把更多数据送修复；如果修复成本高，79% 是合理平衡。关键不是数字本身，而是分层处理后的净收益。

**Q3：LLM 修复会不会把错数据修得更错？**
会，所以修复后有回流验证（Gate 2 重跑），修复失败的直接废弃不进交付。这就是级联设计的价值：LLM 的不可靠性被验证环节兜底。

**Q4：黑盒 20% 的配比依据？**
5% 太少：统计推断不稳定、防过拟合弱；50% 太多：白盒不够，无法做推理错误分析。20% 是统计显著性和诊断价值的平衡点（数学 582 条黑盒 → 95% 置信度误差 ±4%）。

**Q5：学科标签纠错的硬规则覆盖多少？**
两步漏斗：硬规则处理确定 case（关键词匹配），LLM 处理模糊/跨学科 case，两者分歧人工仲裁。硬规则覆盖比例和 LLM 覆盖比例是需要补的量化数据（待深挖）。

**Q6：这条经历和"评测集构建"什么关系？**
质检规则 = 评测标准定义，5 层 Gate = 分级评分体系（通过/修复/废弃三级分类），黑盒配比 = 防泄漏设计。这就是评测集构建的核心工作（任务定义、难度分层、防泄漏）。

**Q7：如果数据量翻 100 倍，你的 Pipeline 瓶颈在哪？**
三层：① 规则层无瓶颈（O(n)）；② LLM 层是瓶颈（每条的推理成本），需要并发 + 降级策略（简单 case 规则优先）；③ 人工仲裁层是绝对瓶颈（需要抽样仲裁 + 置信度机制）。

**Q8：怎么证明你的质检真的让数据变好了？**
终极标准是模型训练效果（Learnability），但短期可用：① 修复前后的规则通过率对比；② 人工抽检的修复质量评分；③ 交付后算法团队的反馈。完整验证需要 Ablation（同一批数据，质检过 vs 不过的模型对比）。

---

## 十、与简历对应

简历子弹点（字节第一条线）已完整覆盖。面试定位：**核心故事**——它是数据质量 + 评测集 + Pipeline 工程三合一，是预训练数据工程师岗位的最强匹配。

相关链接：
- 每日汇报：[2026-07-11](../daily/2026-07-11.md)、[2026-08-03](../daily/2026-08-03.md)
- 知识树：[notebook/0.knowledge-tree.md](../notebook/0.knowledge-tree.md)
- 面试故事库：[interview-prep/story-bank.md](../interview-prep/story-bank.md)

---

## 十一、Evals 方法论落地：质检体系 = Eval System（2026-08-05）

> 背景：之前一直用"质检 / Gate"语言描述自己的工作，没有用 Eval System 语言组织。
> 用 Anthropic 官方 Agent 评测方法论（Task + Trial + Grader）重新表述后，
> 质检经验可以直接迁移到 Agent 评测——这是"Evals 能力"的显式化。

### 11.1 三组件映射

```text
Anthropic Eval System 三组件 → 你的 CoT 质检
├── Task（任务定义）
│   └── 5 层 Gate 的通过标准 + 判定逻辑
│       （格式规则 / 推理链保留标准 / 修复判定标准 /
│        标签一致性标准 / 黑盒配比标准）
├── Trial（试验）
│   └── 每条数据从进入 Pipeline 到离开的状态流转
│       （通过 / 修复回流 / 废弃 / 仲裁）
└── Grader（评分器）
    └── 三层评分器（见下）
```

### 11.2 三个评分器（Anthropic：代码 / LLM / 人工）

| Anthropic 评分器 | 你的实现 | 特点 |
|-----------------|---------|------|
| 代码评分器（确定性） | Gate 1 格式验证 + Gate 2 内容规则 | 零成本、100% 可复现 |
| LLM 评分器（灵活） | Gate 3 LLM Repair 判定 + Gate 4 LLM 标签推理 | 覆盖模糊 case，需回流验证 |
| 人工（黄金标准） | Gate 4 分歧仲裁 + 终审 | 权威，成本最高 |

**这是最核心的对应**：Anthropic 说"评分器分三层，按任务特性选择"，你的质检天然就是三层——说明你不是"做过质检"，而是"实现了 Eval System 的标准架构"。

### 11.3 能力评测 vs 回归评测 → 规则版本管理

```text
Anthropic：能力评测 = 攀登新能力，回归评测 = 防守已有能力
你的场景：
├── 新增质检规则（发现新质量问题类型）
│   └── 属于"能力评测"——探测未知问题，通过率不设预期
├── 稳定规则（已收敛的问题类型）
│   └── 属于"回归评测"——防守已知问题，通过率应保持稳定
└── 实践含义：规则变更要区分"新增探测" vs "收紧标准"，
    两种变更的验收方式不同（前者看能否抓到新 badcase，
    后者看通过率是否如预期下降）
```

### 11.4 评测饱和 → 规则毕业机制

```text
Anthropic：100% 通过的评测用例失去区分度 → 升级难度
你的场景：
├── 某条 Gate 规则连续 N 批通过率 100%
│   └── 它已经不是"质量门"，是"流水线过滤器"
├── 毕业动作：
│   ├── 降级为流水线的强制前置检查（不再计入质量指标）
│   └── 引入更难规则维持区分度（如更深的推理链检查）
└── 面试洞察：规则不是越严越好，而是"保持区分度"——
    这个判断和评测集难度分层是同一个原理
```

### 11.5 部分得分 → 三级分类体系

```text
Anthropic：评测不应只有二元通过/失败，部分得分提升可解释性
你的实现：qualified / needs_repair / discarded 三级分类
├── qualified = 满分通过
├── needs_repair = 部分不合格（有修复价值）
└── discarded = 不可修复（证据缺失/本质错误）
→ 这已经是"部分得分"思想：不是对/错，是"多接近可用"
```

### 11.6 面试话术升级版

```text
❌ 旧： "我设计了 5 层质检规则，筛出合格数据"
✅ 新： "我设计的是一个 Eval System——Task 定义（5 层 Gate 的
   通过标准）、Trial 流程（数据状态流转）、三层 Grader（规则、
   LLM、人工）各司其职。这个架构和 Anthropic 官方 Agent 评测
   方法论同构，所以我理解 Agent 评测的核心设计，也能迁移。
   比如：能力评测 vs 回归评测的区分，指导我怎么管理规则版本；
   评测饱和问题，指导我什么时候升级规则难度。"
```

### 11.7 待补：把同一套语言迁移到 Agent 评测

```text
自驱项目一（Agent 能力评测 Pipeline）还没启动——现在方法论已经通了：
├── Task：5 个评测维度定义（任务完成度/过程效率/指令跟随/错误恢复/代码规范）
├── Trial：10 个真实场景任务
├── Grader：规则（代码可运行性）+ LLM（质量评分）+ 人工（校准）
└── 指标：pass@k / 分级通过率
→ 从"数据质检 Eval System"到"Agent 评测 Eval System"，
  只换评估对象，架构完全复用
```
