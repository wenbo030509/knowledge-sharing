# AI 数据策略：从 AiMe 到 B 端 Agent 平台

> 核心问题：一个 Agent 平台要持续进化，数据策略回答五个问题——
> **采什么、从哪采、怎么用、怎么验证、优先级怎么排**
> 日期：2026-08-24（v2：从流程层重构为数据策略层）
> 状态：🌿 正在深挖中（五问框架已立，待 B 端场景验证 + 工业界持续跟踪）
> 关联文档：`算法反馈迭代` / `AI策略产品与评测能力地图` / `Agent Harness 解剖` / `baidu-agent-migration`（入口-平台架构）

---

## 一、视角校准：流程层 vs 数据策略层

初版笔记的主线是流程：入口 → 打通（MCP/Skills/Tools/Harness）→ 评测闭环。这条线回答的是"系统怎么跑"；数据策略回答的是"系统凭什么越来越强"。

```text
流程层                       数据策略层
───────────────────          ───────────────────────────
入口怎么打通平台              采什么 / 从哪采 / 怎么用 /
数据分两类、评测闭环          怎么验证 / 优先级怎么排
架构是主角                    架构降级为数据的生产系统
数据是输入                    数据是资产，决策链是策略
评测是验收环节                评测是数据投资的回报度量
```

一句话重构：

> **MCP/Skills/Tools/Harness 不是产品架构，是数据策略的生产系统；评测不是验收环节，是数据投资的回报度量。**

面试表述的差别：「我梳理了 AiMe 的架构」vs「我建立了一个 Agent 平台的数据策略决策链，架构只是它的落地」。后者才是策略 PM 的语言。

---

## 二、数据策略五问（核心框架）

```text
数据策略 = 对五个问题的决策链
│
├── 问1 采什么   ← 能力地图 → 数据地图（需求侧）
├── 问2 从哪采   ← 来源组合策略（供给侧）
├── 问3 怎么用   ← 消费路径分流（训练/RAG/抽象）
├── 问4 怎么验证 ← 评测 = 数据投资的回报度量
└── 问5 优先级   ← 数据预算分配
```

### 问1 采什么：能力地图 → 数据地图

数据策略的起点不是"有什么数据"，而是"要什么能力"。**数据需求是能力需求的镜像**：

```text
产品能力地图 ──反推──→ 数据需求规格
│
├── 问答能力（准确性）→ 知识型数据规格：
│     哪些领域？哪些模态？哪些问题类型（事实/推理/检索）？
├── 操作能力（效率）→ 行为型数据规格：
│     哪些平台？哪些任务类型？成功/失败轨迹配比？
└── 编排能力（长程自主）→ 长程轨迹规格：
      多步任务？依赖关系？中断恢复？
```

**和你已有实践的同构**：这一步就是 VLM 项目里的"标准文档拆解"（能力维度定义 → 选材约束 → 交付格式）——**数据需求规格 = 标准文档的 Agent 平台版**。数据策略 PM 定义"什么数据算合格"，就定义了"模型能学什么"——这是 Learnability 的决策权。

### 问2 从哪采：四种来源的组合策略

| 来源 | 真实性 | 定向性 | 成本 | 风险 |
|------|--------|--------|------|------|
| 生产轨迹（真实使用） | 最高（真实分布） | 低（自然流量偏斜） | 低（免费产出） | 幸存者偏差、隐私/授权 |
| 人工标注/众包 | 高 | 最高（定向造样本） | 高（贵、慢） | 标注者偏差 |
| 合成数据 | 中 | 高（可定向） | 中（算力） | 分布失真（模型自产自销） |
| 用户纠正数据 | 最高 | 高（天然对错标签） | 低（生产副产品） | 量不稳定、需埋点采集 |

**用户纠正数据值得单独点名**：用户在 Agent 出错时手动接管/纠正，生产环境免费产出的"错误路径 vs 正确路径"偏好对——介于生产轨迹和人工标注之间的第四种来源，是 Agent 平台特有的高质量数据红利。

**组合策略随产品阶段演进**：

```text
冷启动期：人工标注为主（定向覆盖能力缺口）+ 合成扩量
增长期：生产轨迹为主，人工标注转向"纠错/仲裁/失败补采"
成熟期：生产轨迹 + 用户纠正 + 合成失败（补负样本分布）
```

### 问3 怎么用：消费路径分流（不是所有数据都要训练）

```text
数据 → 三条消费路径
├── 训练（SFT/RL）      → 泛化能力：慢、贵、不可控，但上限高
├── RAG（检索）         → 知识注入：快、可更新、可追溯
└── 抽象（Skills/Tool） → 确定性能力：快迭代、可审计，但天花板受模型限制
```

| 判断维度 | 训练 | RAG | Skills/Tool 抽象 |
|---------|------|-----|-----------------|
| 知识变化频率 | 低频稳定 | 高频更新 | 流程固定 |
| 需求性质 | 泛化（没见过的变体） | 事实召回 | 确定性操作序列 |
| 迭代成本 | 高（重训、慢） | 低（更新知识库） | 低（改文档/配置） |
| 可控性 | 低（黑盒） | 高 | 高（可审计、可回滚） |

**路线迁移也是策略**：Skills 大规模沉淀验证后 → 训练固化进模型（推理侧精简 skills）；知识更新频率下降后 → RAG 知识微调进权重（降低检索延迟）。分流不是一次性的，是数据资产的动态路由。

### 问4 怎么验证：评测 = 数据投资的回报度量

```text
投资前：数据需求规格（这轮投什么、预期影响哪个能力、预期指标）
投资中：单变量原则——一次只改一类数据，同评测集前后对比
投资后：问答评测（事实性/相关性）+ 使用评测（任务成功率/工具准确率/效率）
护栏：难度坍缩、覆盖换信噪比（指标涨但实际没变好）
在线：行为信号（完成率/重试率/人工接管率）——离线涨 ≠ 真实好用
归因 → 下一轮采什么（数据飞轮闭合，连接《算法反馈迭代》）
```

**数据策略的 ROI 语言**：每类数据的单位能力收益（这类数据投 1 单位预算，对应能力涨多少）。这是策略 PM 和算法团队对齐预算的语言（连接 ROI Estimation 节点）。

### 问5 优先级：数据预算分配

```text
数据预算 = 标注人力 + 合成算力 + 采集工程 + 存储
│
├── 能力缺口优先：评测显示最弱的维度先投
├── 边际收益递减：同类数据加量到收益递减 → 转向高难度样本/新类型
├── 快赢 vs 慢赢：RAG 当天见效（止血）vs 训练以周计（治本）
│     先快赢攒信任，同步攒数据，攒够再慢赢
└── 战略储备：预判能力演进方向提前布局数据（VLM 演进史的经验：
      2026 多视频/长视频数据 = 提前卡位行业空白）
```

---

## 三、案例演练：AiMe「数据分析」能力升级（应用示范）

把五问串成一个完整决策链——假设你是 AiMe 数据策略 PM，评测报告显示：**用户用 AiMe 分析数据时，Agent 经常选错取数口径**。

```text
步骤 0 评测归因：工具调用正确率低——区分"模型笨"vs"不懂业务口径"
      （证据：换一个口径定义清晰的 BI 平台，正确率回升 → 不是能力问题，是知识问题）

问1 采什么：
├── 口径知识：指标定义文档（每个指标的业务含义、计算逻辑）
└── 轨迹偏好对：错误取数路径 → 用户纠正后的正确路径

问2 从哪采：
├── 现成 BI 文档（零成本，RAG 直接用）
├── 数据团队标注口径样本（定向，覆盖高频指标）
└── 用户纠正轨迹（生产副产品，埋点采集）

问3 怎么用：
├── 口径知识 → RAG（口径更新快，训练跟不上变化）
└── 纠正轨迹 → SFT/DPO 偏好对（教行为：什么是错误取数、什么是正确取数）

问4 怎么验证：
├── 问答评测：口径类问题准确率（如"GMV 的定义"）
├── 使用评测：取数任务成功率 + 重试率 + 人工接管率
└── 护栏：口径问题不能靠"少接复杂任务"刷成功率（难度坍缩）

问5 优先级：
├── 先上 RAG 快赢（一周内见效，止血）
├── 并行攒纠正轨迹（埋点已就绪）
└── 攒够量再训练（慢赢，治本）

迭代：下一轮评测显示口径问题下降，但多口径交叉分析仍错
     → 归因 → 下一轮采"跨表多口径"类轨迹，难度上移一级
```

这个案例就是数据策略的完整形态：**每一步都是决策，每一步都有依据和验证**。

---

## 四、基础设施：统一入口四层架构 = 数据生产系统

架构仍然重要，但在数据策略视角下重新定位——它是五问的落地工具，不是主角：

```text
统一入口（AiMe）
│
├── L0 Harness       → 数据采集器：可观测性（每步 tool call 日志）= 轨迹数据的地基
├── L1 MCP/CLI       → 降低数据来源接入成本：N×M → N+M（平台接入一次，全员消费）
├── L2 Skills        → 数据的编码形态：操作知识沉淀 = 一种"数据产品"
│                     （手工编写 = 冷启动；轨迹蒸馏 = 数据飞轮的出口）
└── L3 Tool 定义     → 数据质量的第一道 gate：契约模糊 → 轨迹全是噪声
```

**战略支点（策略视角）**：统一入口的价值不只是使用成本，更是**数据归集位置**——所有平台的使用轨迹天然流经入口。谁在入口，谁拥有使用数据，谁的 Agent 迭代最快。这是"统一入口"值得做的数据层理由，不是 UI 层理由。

---

## 五、数据质量与偏差：轨迹数据的幸存者偏差

数据策略必须处理质量与偏差问题，最典型的是生产轨迹的**幸存者偏差**：

```text
生产环境轨迹天然偏斜：
├── 成功轨迹多（用户完成了才继续用）
├── 失败轨迹少（失败即中止、换人干）
└── 直接拿生产日志训练 = 模型只见成功不见失败，学不会"什么不能做"

对策（数据策略动作，不是工程动作）：
├── 失败日志专项挖掘：failures 是负样本金矿
├── 用户纠正数据采集：埋点设计保障（问2 的第四种来源）
├── 合成失败：人为注入错误场景补分布
└── 主动采样：按任务类型/难度分层采样，不按流量（连接 Stratified Sampling）
```

---

## 六、B 端应用：冷启动、回流与壁垒

五问在 B 端的变形：

| 维度 | 内部 AiMe | B 端 Agent 平台 |
|------|----------|----------------|
| 问2 来源 | 生产轨迹量大免费 | 冷启动：手工 Skills + 少量定向标注；生产数据依赖客户回流 |
| 回流条件 | 内部日志全量可用 | 客户授权 + 脱敏 + 合规（数据不出域/私有化） |
| 问4 验证 | 内部评测体系可建 | ground truth 稀缺 → 行为信号（完成/重试/接管）+ LLM judge 兜底 |
| 壁垒 | 迭代速度 | 客户使用轨迹积累 = moat（模型同质化时代，数据差异化是唯一非模型壁垒） |

**B 端数据策略要点**：

```text
1. 冷启动：手工 Skills（行业 know-how 编码）+ 定向标注少量高质量轨迹
2. 回流设计：数据回流是谈判题不是工程题——客户为什么愿意给数据？
   （价值交换：给出数据 → 拿到更懂自家业务的 Agent；或私有化让数据不出域）
3. 壁垒逻辑：每多一个客户多一分轨迹积累，先发者的数据优势
   无法被纯模型能力抹平
4. Harness 落地：可插拔（新客户系统接入成本低）+ 稳定（企业级 SLA）
   + 可观测（数据回流的前提——没有埋点就没有回流）
```

---

## 七、角色代入：数据策略 PM 的三件事

把上面的框架翻译成岗位动作（这就是"应用"的落点）：

```text
1. 建数据需求地图
   能力 → 数据规格：定义每类能力需要什么数据、什么算合格
   （与 VLM 标准文档拆解同一方法论，直接复用）

2. 建采集与质量方案
   埋点设计（哪类轨迹要采、用户纠正怎么采）
   + 失败样本专项采集
   + 质量 gate（Cascading Classification 思维复用：
     确定性规则 → LLM 校验 → 人工仲裁）

3. 跑评测闭环、做采数决策
   归因 → 下一轮采什么、投多少、预期涨什么指标
   （单变量验证 + 护栏指标 + ROI 语言）
```

---

## 八、工业界对照（简要）

- **MCP**：2024.11 Anthropic 开源，2025 OpenAI/Google/Microsoft 相继支持——连接层（问2 的接入成本）行业共识已形成
- **Salesforce Agentforce**：Skills（动作封装）+ Topics（意图路由）——问3 分流的企业端实践：80% 确定性场景走抽象，模型只负责编排；壁垒在客户 CRM 数据
- **Anthropic context engineering 理念**：context engineering > fine-tuning——与问3 分流同构，先编码进上下文/工具，泛化缺口才留给训练
- **扣子 Coze**：插件/工作流/知识库三件套 = 连接 + 抽象 + 知识的产品化
- **你的 price-agent**：B 端最小实践场——现在缺的不是代码，是按五问补上数据规格、轨迹采集和评测闭环

---

## 九、四视角速查（策略视角为主）

```text
PM 视角
├── 数据需求规格 = 标准文档的 Agent 平台版（复用已有方法论）
├── 用户纠正数据 = Agent 平台特有的数据红利，埋点是产品决策
└── 快赢（RAG）vs 慢赢（训练）的节奏设计 = 产品排期逻辑

算法视角
├── 两类数据 = 知识型（SFT/RAG）vs 行为型（agentic SFT/RL）
├── 指标：任务成功率 / 工具调用准确率 / 轨迹效率 / 风险护栏
└── 轨迹幸存者偏差 + 偏好对（纠正数据）的价值

工程视角
├── Harness 可观测性 = 数据采集地基（没有日志就没有策略）
├── N×M → N+M 接入成本模型
└── B 端：可插拔 + 稳定 + 埋点（数据回流的工程前提）

策略视角（主线）
├── 五问决策链：采什么 / 从哪采 / 怎么用 / 怎么验证 / 优先级
├── 来源组合随阶段演进（冷启动 → 增长 → 成熟）
├── 评测 = 数据投资的回报度量（ROI 语言）
└── 预算分配：能力缺口优先 + 边际收益 + 快赢慢赢 + 战略储备
```

---

## 十、讲述故事（数据策略版）

**30 秒版**：

> 我用数据策略回答"一个 Agent 平台怎么持续进化"——五个问题：采什么、从哪采、怎么用、怎么验证、优先级。能力需求反推数据地图：问答能力要知识型数据，操作能力要轨迹数据；来源上生产轨迹真实但偏斜、标注可控但贵、合成快但有分布风险，按阶段组合，用户纠正数据是 Agent 特有的红利；消费路径分流：变化快走 RAG、流程固定抽象成 Skills、能力缺口才训练；两套评测闭环度量数据投资的回报；预算按能力缺口和边际收益分配。以 AiMe 为例，MCP/Skills/Tools/Harness 这套架构，本质是数据策略的生产系统。

**2 分钟版**：五问框架 → AiMe 数据分析案例演练（归因 → 五问完整决策链 → 迭代）→ 轨迹幸存者偏差与用户纠正数据 → B 端变形（冷启动靠手工 Skills、壁垒靠客户轨迹积累、回流是谈判题）→ 我的落点（VLM 标准文档拆解方法论直接复用 + price-agent 按五问补齐）。

---

---

## 十二、Next Level：从任务工具到自驱组织

### 现状判断：市面平台能力已收敛

AiMe、Coze、Agentforce、Copilot Studio 的能力面收敛到同一套：统一入口 + MCP 连接 + Skills + Tool use + Harness + 轨迹数据 + 任务级评测。**收敛本身就是信号——next level 不在这个面上。** 这套能力的本质是"任务型工具"：人发起 → agent 执行 → 人验收。三个没解决：

```text
├── 状态断：任务之间无记忆，每次都从头开始
├── 飞轮没闭合：轨迹沉淀靠人写 skills、归因靠人做、采数靠人决策
└── agent 没有职责只有任务：不能被委派一个持续目标
```

### 四个升级向量

```text
向量 A 交互模式：被动调用 → 自主持续（event-driven / 长期运行）
向量 B 组织形态：单 agent → agent 组织（分工 / 协作协议 / 市场治理）
向量 C 进化机制：人工迭代 → 飞轮自驱（轨迹蒸馏 / 记忆 / 知识自更新）
向量 D 信任体系：任务评测 → 绩效评测（持续指标 / 审计 / 接管协商）
```

### 功能清单（每个向量能实现什么）

```text
向量 A（自主持续）
├── 定时/条件触发任务："每天 9 点生成数据日报，异常自动告警"
├── 监控型 agent：持续盯数据/系统，条件满足自主行动
├── 长期运行任务：跨天任务、等待外部事件再继续
└── 预判式建议：从使用轨迹预测"你现在可能要 X"
    （轨迹数据的新用途：不只是训练素材，是需求预测信号）

向量 B（agent 组织）
├── 专用 agent 团队 + 编排：写码/数据/文档 agent 各自深耕、互相委派
├── A2A 协作协议（Google 2025.4）：agent 间任务委派标准化
├── Skill/Agent 市场：第三方开发、平台做分发与质量治理
└── 人升级为管理者：分配职责、看绩效、回收权限

向量 C（飞轮自驱）
├── 轨迹蒸馏引擎：高频成功轨迹自动挖掘新 skills，人只审核不编写
├── 跨任务记忆：用户偏好/项目上下文自动积累，新任务自动携带
├── 知识自更新：agent 发现知识过期，自动回写知识库（self-healing）
└── 自动归因：bad case 自动分类到根因 → 直接生成下一轮采数建议

向量 D（绩效评测）
├── 持续绩效面板：成功率/效率/成本/风险四维，跨任务统计
├── 自动归因 + 迭代建议：评测结果直接驱动 skills/训练/预算
├── 审计与回放：agent 每一步决策可追溯（信任基础设施）
└── 接管与协商：不确定时主动问人；人的纠正即时进入飞轮
```

### 设计原则（两条）

**1. 自主性每放开一层，评测兜底深一层**（百度反刍"自主性分层 ← → 三层评测"的直接升级版）：

```text
任务级自主（现状）  → 任务级评测
持续职责自主        → 绩效级评测（长期指标 + 护栏）
组织级自主（agent 管 agent）→ 治理级评测（审计 / 权限 / 责任边界）
```

**2. 数据飞轮闭合是 next level 的引擎**：现在飞轮上的人工环节（写 skills、归因、采数决策）都要自动化——谁先闭合，谁先进入自驱进化。模型同质化时代，next level 的竞争不是谁的模型强，是谁的飞轮转得快。

### 工业界信号（方向验证）

- Google A2A（2025.4）：向量 B 的协作协议已标准化
- OpenAI scheduled tasks / Manus 长任务：向量 A 的早期形态
- Agentforce 2.0 + AgentExchange：数字员工叙事 + 市场治理（向量 B/D）
- Sierra：supervised autonomy——人管结果不管过程（向量 D 的接管机制）
- Anthropic Skills 生态：向量 C 的能力封装基础

### 你的位置：next level 的核心岗位仍是评测×数据

四个向量里 B/C/D 的引擎都是评测 + 数据：

```text
市场治理 = 评测体系（skills 好坏谁说了算）
绩效评测 = 评测体系升级（从任务级到持续级）
飞轮闭合 = 数据策略自动化（归因 → 采数 → 蒸馏）
→ "评测驱动的策略产品"在 next level 平台里是引擎岗位，不是配角
```

---

## 待深挖方向

- [ ] 轨迹数据 → 可训练样本的格式转换（agentic SFT 数据构造）
- [ ] 在线评测行为信号指标体系的定义与归因（完成率/重试率/接管率）
- [ ] B 端客户数据回流的合规与激励设计（价值交换的具体方案）
- [ ] Skills 从轨迹蒸馏的工程方法
- [ ] 数据预算分配的量化模型（单位能力收益怎么算）
- [ ] Next Level 四个向量逐条做八层解剖（选 1-2 条最贴岗位的）
- [ ] 绩效级评测的指标设计与防 Goodhart 设计（长期指标怎么不被刷）
